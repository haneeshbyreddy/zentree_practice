#include <logger.h>
#include <initializer_list>

struct ListNode {
    int val;
    ListNode* next;
};

int main() {
    ListNode* head = nullptr;
    ListNode* tail = nullptr;

    for(int i : {1,2,4,7,9}) {}
    return 0;
}